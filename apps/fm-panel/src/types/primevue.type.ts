export enum Severity {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warn',
  DANGER = 'danger',
  CONTRAST = 'contrast',
}

export enum InputType {
  TEXT = 'text',
  NUMBER = 'number',
}
